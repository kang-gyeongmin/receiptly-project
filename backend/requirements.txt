# ── Phase 1 (현재) ─────────────────────────────────────
fastapi==0.115.0
uvicorn==0.30.6
motor==3.5.1               # MongoDB 비동기 드라이버
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
python-multipart==0.0.12   # 파일 업로드

# ── Phase 2 (OCR + LLM) ────────────────────────────────
# boto3==1.35.0             # AWS SDK (Textract, Bedrock)
# langchain-aws==0.2.0
# langchain-core==0.3.0
# pillow==10.4.0            # 이미지 처리

# ── Phase 3 (PostgreSQL + Polars + RAG) ────────────────
# sqlalchemy==2.0.35
# asyncpg==0.29.0           # PostgreSQL 비동기 드라이버
# alembic==1.13.3           # DB 마이그레이션
# polars==1.7.0
# faiss-cpu==1.8.0          # 벡터 검색
# langchain-community==0.3.0

# ── Phase 4 (Airflow는 별도 환경) ──────────────────────
# apache-airflow==2.10.0    # docker-compose로 별도 실행